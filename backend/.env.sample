# Production Environment Variables for Render

# Database - PostgreSQL Connection
# Replace with your actual database URL from Render or other provider
DATABASE_URL="postgresql://username:password@hostname:port/database_name?sslmode=require"

# JWT Secret (Generate a new one for production)
JWT_SECRET="generate_a_secure_random_string_for_production"

# Email (Not needed if skipping email verification)
# If using email verification, uncomment and fill these
# EMAIL_FROM="your-email@example.com"
# RESEND_API_KEY="your_resend_api_key"

# Frontend URL (Update with your Vercel frontend URL)
FRONTEND_URL="https://your-app-name.vercel.app"

# Server
PORT="10000"  # Render assigns a port via this environment variable

# Production Settings
NODE_ENV="production"
SKIP_EMAIL_VERIFICATION="true"  # Set to false if implementing email verification